# Cashier Application for Visual Programming (Pemrog. Visual)
## List of contents:
 - Introduction
 - Technology used
 - Dependency
 - How to install
 - Run the program
 - Open the program
   - List user
- Role explanation


## Introduction
This repository is a project from the results of my Visual Programming (UKK), in this project, i'm Muhammad Nabil Faiz Amrullah got a project with the title cashier system application, where the application must have the following features:
  - Login process
  - Add users
  - Add customers
  - Add products
  - Transaction
      - Sale
  - Report income
  - Report stocks

## Technology used
This application was created using several technologies, including:
    
> [!NOTE]
> _Codeigniter_ here functions as a backend framework
> 
> _Bootstrap_ functions as a template for the frontend framework

- `Codeigniter` version 4.4.8
- `Bootstrap` version 5.3
- `Sweet Alert` version 11.17.2
- `jQuery` version 3.4.0
- `MDI Icon` version 7.4.47

## Dependency

> [!NOTE]
> PHP, MySQL and Apache can be installed with a bundle such as _XAMPP_
>
>  _Git_ functions to clone or download this repository
   
- `PHP` 8.2.12
- `MySQL` 5.2.1
- `Composer` 2.8.6
- `Apache`
- `Git`

## How to install 
### If you wanna download this folder follow this step:
- Download this repository (When downloaded it will be in zip form)
- Unzip the kasir-visual folder

### If you wanna clone follow this step:
- Open the terminal or command line. Run the following command:
 ```
  git clone https://github.com/Aaronabil/kasir-visual.git
  ```
- This will download the kasir-visual folder

> [!NOTE]
> Save the kasir-visual folder in any location
- Create database kasir then import database
- Rename the env file to .env (starting with a dot)
- Type ```composer update```

## Run the program
- Open Visualstudio Code
- Navigate to the 'kasir-visual' folder
- Open the terminal or click **ctrl + `**
- Run the php spark serve command then press enter

## Open the program
open a browser then access the URL http://localhost:8080 then the program will appear like this:

### List user
